{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "outDir": "./dist"
  },
  "exclude": ["node_modules", "test", "dist", "**/*.spec.ts"],
  "// CRITIC PASS": "File build minimale; TODO includere configurazioni per bundle separati (es. worker BullMQ) e ottimizzazione incremental build."
}
