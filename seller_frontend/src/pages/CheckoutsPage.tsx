import { CheckoutsSection, ReferralCard } from '../features/kit/sections';

export default function CheckoutsPage() {
  return (
    <div className="grid gap-6">
      <ReferralCard />
      <CheckoutsSection />
    </div>
  );
}
